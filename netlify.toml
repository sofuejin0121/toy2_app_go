[build]
  base    = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "20"

# React Router 用: 全ルートを index.html にフォールバック
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
